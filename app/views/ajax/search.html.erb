<%= form_tag({ action: 'result' }, remote: true, id: 'ajax_form') do %>
  <% options = options_from_collection_for_select(@books, :publish, :publish,
                                                  'SBクリエイティブ') %>

  <%= select_tag(:publish, options) %>
  <%= submit_tag '検索' %>
<% end %>
<ul id="result"></ul>
